# Chronicle Engine v3.0 — Dependencies
# Install: pip install -r requirements.txt

# Telegram bot framework (with job-queue scheduler built in)
python-telegram-bot[job-queue]==21.5

# PostgreSQL (replaces MongoDB)
psycopg2-binary>=2.9.0

# AI inference (NLP parsing + AI chat)
groq>=0.9.0

# Environment variables
python-dotenv>=1.0.0

# Calendar / iCal parsing (AITU LMS integration)
icalendar==5.0.13

# Timezone handling
pytz==2024.1
